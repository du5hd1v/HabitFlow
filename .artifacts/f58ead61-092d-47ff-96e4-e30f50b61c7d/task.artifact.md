# Task List - HabitFlow AI Planner, Pomodoro Service & ViewModels

- [x] Implement `PreferencesManager` for Gemini API Key storage via DataStore
- [x] Create `TimerForegroundService` for background countdown timer and notification controls
- [x] Create `PomodoroWorker` using WorkManager for daily reminders
- [x] Implement `HomeViewModel` for habits, XP, and streak tracking
- [x] Implement `StudyViewModel` for Pomodoro timer and ambient audio presets (+100 XP reward)
- [x] Implement `AiPlannerViewModel` for Gemini prompt dispatch and adding tasks to DB
- [x] Implement `StatsViewModel` for analytics, weekly XP, and focus hours
- [x] Verify build successfully (`./gradlew :app:assembleDebug`) and run unit tests
